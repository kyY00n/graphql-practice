spring:
  data:
    r2dbc:
      repositories:
        enabled: true

  r2dbc:
    driver: mysql
    url: r2dbc:h2:mem:///player?options=DB_CLOSE_DELAY=-1
    username:
    password:
    pool:
      enabled: false